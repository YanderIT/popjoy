import { CheckoutForm } from '@/shared/blocks/shop/checkout-form';

export const metadata = {
  title: 'Checkout',
  description: 'Complete your purchase',
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
